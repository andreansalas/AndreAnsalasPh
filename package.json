{
  "name": "coderhouse",
  "version": "1.0.0",
  "description": "paquete 28115",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1", "build-css": "node-sass --include-path scss main.scss main.css",  "watch-css": "nodemon -e scss -x \"npm run build-css\""
  },
  "author": "\"Andrea Ansalas\"",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.1",
    "nodemon": "^2.0.15"
  }
}
